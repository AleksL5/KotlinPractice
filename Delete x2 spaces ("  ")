fun main() {
    val message = "Всегда  есть два, не  больше,   не меньше.   Мастер  и  ученик  —  Йода."
    val result = message.split(" ").filterNot { it.all { it == ' ' }}.joinToString(" ")


    println(result)
}

// result = message.replace(Regex("\\s+"), " ").trim()
